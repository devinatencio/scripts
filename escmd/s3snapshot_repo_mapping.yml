# S3 Snapshot Repository Mapping Configuration
# This file defines which Elasticsearch clusters should have elastic_s3snapshot_repo configured
# Format: cluster_name: repository_name

# Production clusters with S3 snapshot repositories
aex10-c01: aex10-repo
aex20-glip: aex20-repo
str04-c01: str04-repo
syd12-c01: syd12-repo

# Add additional cluster to repository mappings below as needed
# Example:
# cluster-name: repo-name
